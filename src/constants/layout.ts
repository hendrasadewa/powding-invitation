export enum AVAILABLE_LAYOUT {
  SIMPLE = 'SIMPLE',
}

export enum AVAILABLE_WIDGETS {
  EVENT = 'event',
  QUOTE = 'quote',
  BRIDE = 'bride',
  RUNDOWN = 'rundown',
  GALLERY = 'gallery',
  RESERVATIONS = 'reservations',
  GIFT = 'gift',
}
